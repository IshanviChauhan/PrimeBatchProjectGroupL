import { Router } from 'express';

const router = Router();

// Placeholder routes - will be implemented based on community features
router.get('/', (req, res) => {
  res.json({ message: 'Users routes working' });
});

export default router;
